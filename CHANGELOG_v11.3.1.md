# v11.3.1 메뉴·홈·지원자 목록 화면 밀도 개선

## 사이드바

- 데스크톱 확장 폭을 224px로 조정하고 메뉴 버튼은 데스크톱 36px·모바일 44px 이상을 유지했습니다.
- 기존 그룹 접기·펴기, 축소 사이드바와 모바일 메뉴 닫기 동작을 그대로 사용합니다.
- `storageNote`와 `authNote`를 하나의 시각적 상태 영역으로 묶었습니다. 운영·저장 상태 설명은 `storageNote`가 담당하고 인증 영역은 이메일 또는 로그인·로그아웃 동작만 표시합니다.
- 클라우드 실패만 경고색으로 표시하며 동기화 중·로그아웃·회사 로컬·공용 저장 상태와 상단 요약 배지는 기존 판정 흐름을 유지합니다.

## 홈

- 중복 인트로를 숨기고 누적 KPI를 전체 지원자, 진행중, 이번 달 지원, 입사예정 4개로 정리했습니다.
- 오늘 면접, 기한 경과, 연락 필요, 판정 필요, 입사 임박은 오늘 처리할 업무 카드에서만 표시합니다.
- KPI와 업무 카드의 기존 키보드·클릭 화면 이동을 유지했습니다.

## 지원자 목록

- 데스크톱 상단을 요약, 검색·정렬·동작, 상태·근무지 빠른필터의 3개 시각 행으로 압축했습니다.
- 필터의 기능·카운트·data 속성은 유지하고 좁은 폭에서는 해당 영역 안에서 줄바꿈 또는 가로 스크롤합니다.
- 표의 강제 최소 높이를 제거하고 넓은 표는 기존 `table-wrap` 안에서만 스크롤합니다.
- 검색 결과가 없으면 240px 이하의 빈 상태에 필터 초기화와, 쓰기 권한이 있을 때만 지원자 등록 동작을 제공합니다.
- 일반보기·워크시트 전환과 워크시트 저장 안전바는 변경하지 않았습니다.

## 데이터 안전·검증

- 화면 이동·렌더·필터·사이드바 토글이 지원자·사원·학교 배열과 업무 localStorage를 변경하지 않는지 검사합니다.
- 최소 60명의 합성 지원자로 1366×768 일반보기에서 표 헤더와 3행 이상 노출, 상단 3행, 본문 overflow 1px 이하, 표 내부 가로 스크롤을 검사합니다.
- 1920×1080, 1440×900, 1366×768, 1024×768, 768×1024, 390×844와 1366×768 125% 확대 화면을 합성 데이터로 검사합니다.
- Supabase·DB·권한 모델·ERP Bridge·shared-storage·공용 master·업무 스키마·지속 저장 키는 변경하지 않았습니다.
