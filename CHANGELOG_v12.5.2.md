# v12.5.2 — 면접 명단표 긴급 인쇄 복구

## 변경

- 인쇄 버튼을 여러 번 눌러도 `window.print()`가 중복 호출되지 않도록 잠금을 추가했습니다.
- 숨겨진 명단표를 화면 밖에서 먼저 배치하고 레이아웃을 계산한 뒤 인쇄를 호출하도록 변경했습니다.
- 인쇄 완료·취소·호출 실패 뒤 인쇄 상태와 버튼 상태를 안전하게 복구합니다.
- 인쇄 호출이 실패하면 무응답으로 남지 않고 재시도 안내를 표시합니다.

## 유지되는 평가표 기준

- A4 가로와 5명 단위 페이지 분할
- 회사 평가표 고정 문구와 자동채움 항목
- 순흑색 0.50mm 독립 격자선
- 날짜별 저장 순서와 신규 지원자 후순위 규칙

## 데이터 안전

- 지원자 데이터와 LOCAL ONLY 저장 구조를 변경하지 않습니다.
- localStorage·IndexedDB 안전 복사·스냅샷 정책을 변경하지 않습니다.
- 자동검사는 합성 지원자 정보만 사용합니다.
