# Recruit ERP v10.53.0

## 클라우드 동기화 안전 개선

- 지원자·사원·학교 자료에 공통 동기화 기준점을 기록합니다.
- 한쪽에서만 수정된 자료는 수정된 쪽을 선택하고, 두 브라우저에서 모두 다르게 수정된 자료만 충돌로 분류합니다.
- 충돌이 발생하면 클라우드 값을 조용히 덮어쓰지 않고 이 브라우저 값을 유지하면서 양쪽 사본을 최대 50건까지 로컬에 보관합니다.
- 동기화 상태 안내의 `충돌 확인` 버튼에서 이 브라우저 정보 또는 클라우드 정보를 직접 선택해 반영할 수 있습니다.
- 클라우드 저장 실패 시 대상 ID를 재시도 대기열에 보관하고, 로그인 직후·인터넷 연결 복구 시·`다시 시도` 버튼으로 전송을 다시 시도합니다.
- 같은 자료의 클라우드 저장 요청이 동시에 겹치면 먼저 진행 중인 저장을 끝낸 뒤 다음 변경을 전송해 저장 순서가 뒤섞이지 않게 했습니다.
- 화면 표시와 정적 자원 버전을 `v10.53.0`으로 갱신했습니다.

## 데이터 보호 원칙

- 충돌 선택 전에는 현재 이 브라우저 자료를 유지하며 클라우드에 자동으로 다시 쓰지 않습니다.
- 충돌 사본과 재시도 대기열은 현재 브라우저에만 저장됩니다.
- 기존 지원자·사원·학교 저장 키와 Supabase 테이블 구조는 변경하지 않습니다.
- Supabase의 공개 스키마 테이블은 기존과 마찬가지로 인증 사용자용 RLS 정책이 적용돼 있어야 합니다.

## 테스트

- `테스트지원자`, `로컬테스트`, `클라우드테스트` 등 가상 값만 사용했습니다.
- 로컬만 변경, 클라우드만 변경, 양쪽 동시 변경, 같은 내용의 시간 차이, 전송 대기 ID 중복 제거를 자동 검사합니다.
- v10.51 저장 안전·사원 Import·CSV 방어와 v10.52 개인정보 보호 회귀 테스트를 함께 실행합니다.

## 알려진 제한

- 처음 v10.53.0을 실행하기 전에는 동기화 기준점이 없으므로 기존과 같이 수정 시간이 최신인 자료를 우선하며, 시간이 같고 내용이 다를 때만 즉시 충돌로 분류합니다.
- 클라우드 삭제 실패 재시도는 이번 대기열에 포함되지 않습니다. 삭제 작업은 기존 로컬 우선 방식과 Supabase 스냅샷 보호를 유지합니다.
