# Recruit ERP v10.46.9.1 QA 보고서

## 1. 대상

- 버전: **v10.46.9.1 HOME_DEV — DESIGN CALM CASCADE CORRECTION**
- 기준본: `recruit-erp-v10.46.8.4-HOME_DEV-FORM_FLOW_STABILITY`
- 보정 근거: `피드백_v10.46.9_검토결과.md`
- 작업일: 2026-07-24

## 2. 종합 판정

**정적 검사 통과 / 브라우저 비교 통과 / 핵심 기능 기준본 동등성 통과**

v10.46.9에서 지적된 CSS 로드 순서 승자 역전 위험을 A안으로 보정했다. 기존 14개 CSS의 순서는 기준본과 동일하며, DESIGN CALM 규칙은 역할이 명확한 최종 계층으로 분리했다.

## 3. 변경 범위 검증

### 파일 구성

- CSS: 15개
  - 기준본 CSS 14개 순서 보존
  - 최종 계층 `css/design-calm-cascade.css` 1개
- JavaScript: 28개
- Supabase 설정 JavaScript: 1개
- HTML ID: 729개

### JavaScript 변경

기준본 대비 변경된 JavaScript는 다음 4개다.

- `js/auth-init.js`
- `js/backup-center.js`
- `js/state-init.js`
- `js/ui-enhancements.js`

나머지 코어 JavaScript 24개는 기준본과 바이트 단위로 동일하다. 변경된 4개 파일은 환경 배지 표시와 버전 문자열 정리 범위이며 데이터·업무 로직을 변경하지 않는다.

## 4. 정적 QA

| 검사 | 결과 |
|---|---:|
| JavaScript 문법 검사 29개 | 통과 |
| CSS 15개 파싱 | 통과 |
| HTML 중복 ID | 0건 |
| 로컬 CSS·JS·이미지 누락 참조 | 0건 |
| 기준본 CSS 14개 순서 보존 | 통과 |
| 최종 CALM 계층 15번째 로드 | 통과 |
| 구 팔레트 `#0f2e74` | 0건 |
| 구 팔레트 `#14b8a6` | 0건 |
| 혼합 포인트 `#1c76ff` | 0건 |
| 죽은 `data-design-system` 속성 | 없음 |
| 배포 폴더 내부 QA JSON | 없음 |
| 구 캐시 버전 참조 | 없음 |

## 5. 기준본 대비 브라우저 비교 QA

동일한 샘플 데이터와 동일한 해상도로 v10.46.8.4와 v10.46.9.1을 각각 렌더링해 비교했다.

### 해상도

- 데스크톱: 1600 × 1000
- 중간: 1024 × 768
- 모바일: 390 × 844

### 비교 화면

- 홈
- 지원자 목록
- 신규 지원자 등록
- 일정관리

총 24개 원본 스크린샷과 12개 좌우 비교 이미지를 생성했다.

### 브라우저 결과

| 검사 | 데스크톱 | 중간 | 모바일 |
|---|---:|---:|---:|
| 페이지 런타임 오류 | 0 | 0 | 0 |
| 콘솔 오류 | 0 | 0 | 0 |
| 전역 가로 넘침 | 없음 | 없음 | 없음 |
| 지원자 목록 표시 열 동등성 | 통과 | 통과 | 카드 모드 통과 |
| 지원자 표 최소 폭 동등성 | 통과 | 통과 | 해당 없음 |
| `list-summary` 계산 스타일 동등성 | 통과 | 통과 | 통과 |
| `table-wrap` 계산 스타일 동등성 | 통과 | 통과 | 카드 모드 육안 확인 |
| 관리 버튼 `상세·수정·삭제` 유지 | 통과 | 통과 | 통과 |
| 문제 열 display 규칙 동등성 | 통과 | 통과 | 숨김 규칙 동등 |

피드백에서 지적된 41개 완전일치 충돌은 기존 CSS 순서 원복으로 구조적으로 승자 역전이 발생하지 않게 했으며, 특히 지원자 목록의 요약 바·표 컨테이너·열 표시를 계산 스타일과 스크린샷으로 재확인했다.

## 6. 핵심 기능 동등성 검사

기준본과 보정본에 동일한 시나리오를 실행했다.

### 신규 등록

- 신규 진입 시 성명·수정 ID 초기화: 통과
- 기본 상태 `서류검토`: 통과
- 출근방법 기본값 `확인필요`: 통과
- 지원일 기본값: 통과

### 수정 및 저장

- 지정 지원자 수정 화면 진입: 통과
- 수정 대상 ID와 필드 로드: 통과
- 신규 지원자 저장 및 localStorage 반영: 통과
- 저장 후 목록 복귀 및 폼 초기화: 통과

### 면접 명단표

선택일 기준 다음 조건을 확인했다.

- `면접예정`: 포함
- `불합격`: 제외
- `면접불참`: 제외
- `면접완료`: 제외

기준본과 보정본 결과가 동일했다.

### 기능 비교 결과

- 기준본·보정본 결과 동등: 통과
- 페이지 오류: 0건
- 콘솔 오류: 0건

## 7. 회귀검사 범위

### 화면·구조 검증

- 지원자 목록: 스크린샷·계산 스타일 비교 완료
- 신규 등록: 화면·기본값·저장 흐름 확인
- 수정: 대상자 로드와 저장 확인
- 상세: 코어 파일 기준본 바이트 동일 및 참조 누락 없음
- 일정관리: 3개 해상도 비교 완료
- 면접 명단표: 포함·제외 조건 실행 확인
- 입사대기자: 코어 JS 기준본 바이트 동일, CSS 파싱·참조 통과
- 백업센터: 저장 로직 기준본 유지, 표시·버전 변경만 확인
- 학교: 관련 코어 JS 기준본 바이트 동일, CSS 파싱·참조 통과
- 사원: 관련 코어 JS 기준본 바이트 동일, CSS 파싱·참조 통과

입사대기자 XLSX 실제 다운로드, 학교·사원 대량 실데이터, 회사망 403 및 집 Supabase 실제 네트워크 저장은 현재 오프라인 QA 환경에서 재현하지 않았다. 해당 로직 파일은 기준본과 동일하며 운영 환경 확인 항목으로 유지한다.

## 8. 알려진 기준본 동작

작성 중 화면 이동 확인 래퍼를 직접 호출하는 자동 시나리오에서는 확인 대화상자가 나타나지 않고 화면이 이동하는 동작이 기준본과 보정본 모두 동일하게 관찰됐다.

- v10.46.9.1에서 새로 발생한 회귀는 아님
- 이번 CSS 캐스케이드 보정 범위에서 수정하지 않음
- 후속 `FORM FLOW STABILITY` 점검 대상으로 유지

## 9. DESIGN CALM 범위

이번 버전은 캐스케이드 안전성 보정과 구 팔레트 정리가 목적이다. 다음은 완료로 주장하지 않는다.

- 그라데이션 전면 제거: 미포함
- 그림자 체계 전면 축소: 미포함
- hover 부양 전면 제거: 미포함
- 타이포 5단계·굵기 3단계 통합: 미포함
- 화면별 CSS 역할 재편: 미포함

위 항목은 기능 안정성과 분리해 v10.47.0 전체 DESIGN CALM 통합 범위로 이관한다.

## 10. 배포 판정

**v10.46.9.1 통교체 검토본 제공 가능**

- 기존 v10.46.9는 배포하지 않는다.
- v10.46.8.4 폴더 위에 부분 덮어쓰기하지 않는다.
- v10.46.9.1 전체 폴더로 통교체한다.
- 운영 투입 전 실제 데이터로 지원자 목록 열·일정 출력·입사대기 XLSX·전체 JSON 백업을 최종 확인한다.
