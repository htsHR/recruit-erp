# v12.2.1 일정관리 평가표 순서 편집 바로가기

- 일정관리의 선택 날짜 패널에 `평가표 순서 편집` 버튼을 추가했습니다.
- 일정관리에서 선택한 날짜를 기존 채용면접 평가표 순서 편집기에 전달하며 별도 편집·저장 로직을 만들지 않습니다.
- 열기·닫기만으로 지원자 배열과 브라우저 저장값을 변경하지 않고, 닫은 뒤 일정관리 실행 버튼으로 포커스를 복원합니다.
- 버튼 3개는 데스크톱에서 자연스럽게 줄바꿈되도록 묶어 1280px 이상과 125% 확대에서도 글자·칸 돌출을 막습니다.
- 모바일·태블릿 신규 UI와 신규 QA는 추가하지 않았습니다.
