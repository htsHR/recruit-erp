# v11.1.0 학교별 인력분석·명단 XLSX 최신화

## PR #17 회사 공용폴더 영구 저장 Preview

- 회사 PC에서 검증된 `ERP Preview → 127.0.0.1 Bridge → 지원팀 공용폴더` 경로를 versioned master 저장소로 확장했습니다.
- `ERP_DATA/erp-data.json`은 revision 기반 동시 저장 충돌을 막고, 임시 파일·flush·재읽기·hash 검증 후에만 master로 교체합니다.
- 정상 master 갱신 전 백업을 만들고 최근 20개를 유지하며, 네트워크 폴더의 일시적 `EBUSY`·`EPERM`은 점진적으로 재시도합니다.
- 모든 `/storage/*` 요청은 Origin 검사와 메모리 전용 Bridge 토큰을 함께 확인하며, 50MB 본문 제한·timeout·단일 쓰기·exclusive lock을 적용합니다.
- 안전 트리·허용 데이터셋·ID·중복·깊이·노드·행 수·문자열 길이를 검사하고, 주민등록번호 필드와 13자리 형태를 공용 snapshot에서 차단합니다.
- 공용 저장소 최초 생성은 사용자 확인 후에만 실행하며, 기존 localStorage가 있는 PC는 공용 master를 확인 없이 적용하지 않습니다.
- 새 PC의 빈 cache에서는 같은 공용폴더의 정상 master를 읽어 지원자·입사대기·사원·학교·일정 등 업무 데이터를 복구할 수 있습니다.
- 이 변경은 Draft PR Preview 전용이며 운영 데이터 초기화, `main` 병합, Production 배포는 포함하지 않습니다.

## 범위

- 최신 기준 파일 `인사DB_(260728).xlsx`를 수정하지 않고 허용된 사원명부·휴직자·퇴직자 시트만 읽어 사번 기준 변경 미리보기를 만듭니다.
- self-closing 빈 셀과 일반 셀을 독립적으로 읽어 다음 성명·날짜 셀이 누락되거나 이동하지 않게 했습니다. 기존 ERP 성명으로 보충하지 않습니다.
- 수식 셀은 저장된 캐시값만 사용하고, 오류 셀과 캐시값 없는 수식은 일반 빈 셀과 구분해 `데이터 확인 필요`로 차단합니다.
- `특수직`은 사번과 학교를 안전하게 연결할 수 있는 행만 검토하며, 연결 근거가 없으면 자동 신규 생성·병합·집계하지 않습니다.
- `전 PM社` 열은 비지원 열로 무시하며 사원 객체, 미리보기, 저장소, 클라우드, 감사로그, 콘솔, 분석 및 내보내기에 포함하지 않습니다.
- 빈 셀은 기존 값을 지우지 않고, 중복 사번·성명 불일치·잘못된 날짜·학교 연결 충돌은 자동 반영하지 않습니다.
- 직책(`position`)과 직책/직무(`role`)를 독립적으로 보존하고 화면·JSON Import·Supabase 동기화·XLSX 내보내기까지 분리합니다.

## 학교 인력분석

- 학교별 누적·연도별 실제 입사와 재직·휴직·퇴직·입사예정을 분리합니다.
- 직급 원문을 유지하면서 직급계열과 숫자 단계를 공통 함수로 동적 계산합니다.
- 직급·조직·채용경로·학력·전공·성별·날짜를 동시에 적용하는 복합필터를 제공합니다.
- 사용자가 고르는 1차원 집계와 2차원 교차분석, 결과 명단, 표시 열, 정렬·페이지 크기를 제공합니다.
- 자주 쓰는 분석조건은 결과나 개인 명단 없이 조건만 브라우저에 저장합니다.
- 현재 조회 결과를 요약·1차 집계·교차분석·명단·확인 필요 시트가 있는 실제 XLSX로 내보냅니다.
- 조회 전용 권한에는 집계만 보이고 개인 명단과 XLSX 내보내기는 표시하지 않습니다.
- 학교명은 정식명·별칭 exact 일치를 우선하고, 후보가 없을 때만 고등교육기관 끝말을 제한적으로 정리합니다. 유일 후보만 연결하며 `영남이공대`·`영남이공대학교`·`영남이공대학`을 같은 학교로 안전하게 찾습니다.

## 안전성과 제한

- 대량 반영 전 안전백업을 만들고 로컬 저장 실패 시 전체 변경을 원상복구합니다.
- 클라우드 일부 저장 실패는 성공·대기 건수를 나눠 표시하고 자동 재시도 대기열에 유지합니다.
- XLSX로 내보내는 `=`, `+`, `-`, `@` 시작값은 일반 문자열로 저장합니다.
- 이번 버전은 기존 `employees`, `schools`, `applicants` 구조를 사용하며 신규 Supabase migration이 없습니다.
- 실제 회사 명단은 저장소, 테스트 자료, GitHub Actions artifact에 포함하지 않습니다.
- 워크북 상단의 `오늘날짜` 오른쪽 셀을 읽어 명단 기준일을 제안합니다. 시트별 날짜가 다르면 적용을 차단하며 파일명 날짜는 기준일로 추측하지 않습니다.
- 파싱이 끝나면 앱은 File·ArrayBuffer·ZIP·공유문자열 참조를 유지하지 않지만, 실제 메모리 회수 시점은 브라우저 가비지 컬렉션에 따릅니다.
