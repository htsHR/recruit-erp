# Recruit ERP v10.60.0

## 암호화 백업·복원

- 기존 `recruit-erp-backup` JSON 전체를 AES-GCM 256비트로 암호화하는 `.erpbackup` 형식을 추가했습니다.
- PBKDF2-SHA-256 310,000회, 16바이트 무작위 salt, 12바이트 무작위 IV를 사용합니다.
- 파일마다 salt와 IV를 새로 만들며 CryptoKey는 추출 불가능하게 생성합니다.
- 암호화 봉투에는 앱 버전과 암호화 방식만 기록하고 이름·연락처·주소·주민등록번호·건수는 기록하지 않습니다.
- 비밀번호는 작업 중 메모리에서만 사용하고 완료·실패·취소 시 입력란을 비웁니다.
- Web Crypto가 없으면 기능을 차단하며 평문 백업으로 자동 전환하지 않습니다.

## 복원 안전장치

- AES-GCM 인증 성공 후에도 복호화 JSON에 기존 안전 트리 검사와 행 ID 검사를 적용합니다.
- `__proto__`·`prototype`·`constructor`, 비정상 객체, 과도한 깊이·노드 수와 위험한 ID는 미리보기 전에 차단합니다.
- 암호화 봉투의 최상위·KDF·cipher 객체는 정해진 키 외의 추가 필드를 허용하지 않습니다.
- 복호화 후 기존 schemaVersion 2, statusSchemaVersion 2, 데이터 건수와 FNV 무결성 검사를 그대로 실행합니다.
- 병합·전체교체·전체복원 미리보기와 재확인 절차를 유지했습니다.
- 암호화 복원 직전에 같은 비밀번호로 현재 ERP 암호화 안전 백업을 내려받습니다.
- 안전 백업 생성 실패 시 적용을 중단하고, 저장 실패 시 모든 데이터셋을 이전 상태로 되돌립니다.
- 기존 평문 JSON 복원은 경고와 함께 계속 지원합니다.

## 권한·감사 기록

- 관리자·로컬 관리자만 암호화 전체 백업과 복원을 실행할 수 있습니다.
- 채용담당자는 기존 정책상 허용된 일반 내보내기만 사용할 수 있고, 조회 전용은 백업·복원이 차단됩니다.
- 회사 모드에서는 암호화 백업 생성만 가능하며 복원·병합·전체교체는 차단됩니다.
- 다운로드, 파일 검사, 미리보기 적용·취소·실패를 민감정보 원문 없이 기록합니다.
- 암호화 내보내기 실패는 `export` 감사로그 1건으로만 기록하며 변경 이력에서 `백업·내보내기` 대상으로 조회할 수 있습니다.
- 정규화 실패 로그에는 데이터셋과 행 번호만 남기며 복호화 행·오류 메시지를 출력하지 않습니다.
- 페이지 이동·화면 잠금·로그아웃·권한 및 운영 모드 변경·pagehide 때 비밀번호와 복호화 미리보기 참조를 폐기합니다.

## 테스트

- 잘못된 비밀번호, ciphertext·salt·IV 변조, 미래 cryptoSchemaVersion, 비정상 base64, 빈 파일과 50MB 제한을 검사합니다.
- 같은 데이터와 비밀번호로 반복 생성해도 파일이 서로 다른지 확인합니다.
- 지원자 5,000명, 사원 1,500명, 학교 500개와 가상 일정·안내문 데이터 성능 시험을 포함합니다.
- Playwright에서 6개 화면 조건, 모바일 팝업 잘림·포커스·Escape, 잘못된 비밀번호, 복원 미리보기와 역할별 차단을 검사합니다.

최종 로컬 결과: JavaScript 58개 문법 검사 통과, 자동 테스트 13개 통과, Playwright 6개 화면 조건·13개 화면·3개 역할 통과. 약 1.04MB 가상 대용량 자료의 기준 PC 처리 시간은 암호화 약 0.20초, 복호화 약 0.18초였습니다. 실제 시간은 PC 성능에 따라 달라질 수 있습니다.

## 변경하지 않은 항목

- localStorage 키와 기존 데이터 필드 구조
- `recruit-erp-backup` 내부 JSON 구조와 schemaVersion/statusSchemaVersion
- Supabase 테이블, migration, RLS 정책
- 기존 동기화·삭제 복구·감사로그 민감정보 비식별화 정책
