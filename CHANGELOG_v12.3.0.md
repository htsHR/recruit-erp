# v12.3.0 첫 화면 스크립트 로딩 개선

- 공장 초기화·보안·안전 설정에 필요한 상단 스크립트 5개는 기존처럼 즉시 실행합니다.
- 본문 JavaScript 45개는 `defer`로 병렬 다운로드하되 기존 선언 순서대로 실행합니다.
- `async`나 실행 순서 변경은 사용하지 않아 기존 전역 의존성과 화면 초기화 순서를 보존합니다.
- 요청 수와 전송량은 늘리거나 줄이지 않고, 순차 다운로드 대기만 완화합니다.
- 기능·저장 구조·업무 데이터·Bridge·Supabase 관련 코드는 변경하지 않았습니다.
- 데스크톱 우선 범위로 진행했으며 모바일·태블릿 신규 CSS와 QA는 추가하지 않았습니다.
