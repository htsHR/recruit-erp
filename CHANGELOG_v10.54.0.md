# Recruit ERP v10.54.0

## 자동 검사

- `npm run check` 한 번으로 전체 JavaScript 문법과 모든 회귀 테스트를 실행합니다.
- 테스트 파일을 추가하면 통합 실행기가 자동으로 찾아 실행합니다.
- GitHub 변경 요청과 `main` 병합 때 GitHub Actions가 같은 검사를 자동 수행합니다.
- 자동 검사는 읽기 전용 권한과 5분 제한으로 실행되며, 같은 브랜치의 오래된 검사는 자동 취소됩니다.

## 배포 보호

- 화면 제목, 왼쪽 버전, 정적 파일 버전이 모두 `v10.54.0`인지 검사합니다.
- 현재 버전 변경 기록이 빠지지 않았는지 확인합니다.
- Vercel의 클릭재킹·콘텐츠 형식·참조 정보·브라우저 권한 제한 설정을 검사합니다.
- 자동 검사와 Vercel 미리보기가 성공한 변경만 운영 `main`에 병합합니다.

## 운영 문서

- `README.md`에 일상 사용, 백업, 동기화 충돌, 안전한 업데이트, 장애 복구 방법을 쉬운 문장으로 기록했습니다.
- 테스트에는 가상 자료만 사용하며 실제 개인정보나 운영 자료를 사용하지 않는 규칙을 명시했습니다.

## 기능 영향

- 지원자·사원·학교 자료 구조와 Supabase 테이블은 변경하지 않습니다.
- v10.51 저장·Import·CSV 보호, v10.52 개인정보 보호, v10.53 동기화 보호 동작은 그대로 유지됩니다.
