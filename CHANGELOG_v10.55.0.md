# Recruit ERP v10.55.0

## 클라우드 삭제 복구

- 지원자·사원·학교 삭제 요청을 브라우저의 안전 대기열에 먼저 기록한 뒤 로컬 자료를 삭제합니다.
- 브라우저 저장공간 문제로 삭제 안전정보를 기록하지 못하면 로컬 삭제 자체를 중단합니다.
- 클라우드 삭제 실패 시 요청 ID, 대상 종류, 요청 시간, 재시도 횟수와 오류를 보관합니다.
- 로그인 직후, 인터넷 연결 복구 시, 사용자가 `다시 시도`를 누를 때 삭제 요청을 자동 재시도합니다.
- 로그인 직후에는 삭제 재시도를 먼저 마친 뒤 클라우드 자료를 불러와 늦게 도착한 응답이 삭제 자료를 되살리지 못하게 합니다.
- 삭제 성공 후에는 관련 저장 재시도와 동기화 기준·충돌 사본을 함께 정리합니다.

## 다시 나타남 방지

- 삭제 대기 중인 ID는 클라우드 자료를 불러올 때 제외하여 실패한 삭제 자료가 화면에 다시 나타나지 않게 합니다.
- 지원자 전체 삭제는 삭제 당시의 ID 목록만 보관·재시도하여, 대기 중 새로 등록한 지원자는 삭제하지 않습니다.
- 삭제 대기 ID는 기존 저장 재시도 대상에서도 제외하여 삭제 자료가 다시 업로드되지 않게 합니다.

## 확인과 취소

- 왼쪽 동기화 상태에 `삭제 대기` 건수를 표시합니다.
- `삭제 대기 확인` 화면에서 대상, 요청 시간, 재시도 횟수와 최근 오류를 확인할 수 있습니다.
- 클라우드 삭제 요청을 취소할 수 있으며, 취소한 자료는 다음 동기화 때 클라우드에서 다시 나타날 수 있음을 안내합니다.

## 테스트

- `삭제테스트지원자`, `가상사원`, `가상학교` 등 가상 자료만 사용했습니다.
- 삭제 대기 저장, 첫 전송 실패 후 성공, 사용자 취소, 전체삭제 우선 처리, 저장공간 실패 시 삭제 차단을 자동 검사합니다.
- 기존 저장·Import·CSV·개인정보·동기화 회귀 검사도 함께 실행합니다.

## 보안

- Supabase 공개 스키마에는 인증 사용자용 RLS 삭제 정책이 계속 적용되어야 합니다.
- 실제 개인정보나 운영 자료는 테스트에 사용하지 않았습니다.
