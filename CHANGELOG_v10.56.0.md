# Recruit ERP v10.56.0

## 화면 보안 강화

- 화면 HTML에 있던 인라인 `onclick`, `onchange`, `onkeydown` 실행 속성을 제거했습니다.
- 허용된 화면 동작만 중앙 `addEventListener`에서 실행하는 선언형 이벤트 방식을 적용했습니다.
- 허용 목록 밖의 함수 이름은 화면 속성에 들어와도 실행하지 않습니다.
- 지원자·사원·학교·일정과 학교 관계 항목의 ID를 안전한 형식으로 정규화합니다.
- 새 ID는 지원 브라우저에서 `crypto.randomUUID()`로 생성합니다.

## JSON 가져오기 보호

- 최대 파일 크기, 최대 자료 건수, 객체 형식과 중첩 깊이를 공통 검사합니다.
- `__proto__`, `constructor`, `prototype` 같은 위험한 속성이 들어간 JSON을 거부합니다.
- 지원자 JSON은 안전한 ID가 모든 항목에 있어야 가져올 수 있습니다.
- 학교·사원·조직정보·전체 백업 검사에도 공통 안전 파서를 연결했습니다.

## CSP와 외부 라이브러리

- Vercel 응답에 Content Security Policy를 적용했습니다.
- 인라인 JavaScript, 인라인 이벤트 속성, `eval()` 계열 실행을 허용하지 않습니다.
- 객체 삽입과 외부 프레임, 허용되지 않은 폼 전송을 차단합니다.
- Supabase JavaScript를 `2.111.0`으로 고정하고 SHA-384 무결성 검사를 추가했습니다.

## 확인

- `<img src=x onerror=alert(1)>` 공격 문자열은 실행되지 않고 일반 글자로 처리됩니다.
- 가상 지원자 ID와 악성 ID, 위험한 JSON 속성, 허용되지 않은 화면 함수 실행을 자동 검사합니다.
- 기존 저장·Import·CSV·개인정보·동기화·삭제 복구 테스트를 함께 실행합니다.

