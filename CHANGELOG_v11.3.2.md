# v11.3.2 지원자 빠른 상세 패널

## 빠른 검토

- 지원자 일반목록의 조작 버튼이 아닌 행 영역을 클릭하거나 Enter·Space로 읽기 전용 빠른 보기를 엽니다.
- 기존 성명·`상세` 버튼은 전체 상세화면, 상태 선택·수정·삭제는 기존 업무 동작을 그대로 유지합니다.
- 이름, 연락처, 이메일, 근무지, 상태, 지원·면접·입사 일정, 지원경로, 경력구분, 출근방법, 학력·학교·전공, 경력·자격증·상담·메모를 명시적 허용목록으로 표시합니다.
- 빈 값은 `미입력`으로 일관되게 표시하고, 사용자 입력은 HTML이 아니라 안전한 문자열로 렌더링합니다.

## 목록 맥락과 반응형 화면

- 이전·다음은 현재 검색·근무지·상태·상세필터·정렬 결과 순서를 그대로 사용합니다.
- 30명 페이지 경계를 넘으면 현재 페이지 표시만 맞추고 검색·필터·정렬·페이지 크기와 목록 스크롤 위치는 유지합니다.
- 선택한 행을 강조하고 첫·중간·마지막 위치에서 이전·다음 버튼을 알맞게 비활성화합니다.
- 데스크톱은 420~480px 오른쪽 패널, 900px 이하 화면은 고정 머리말·작업영역을 가진 전체 너비 시트로 표시합니다.
- Escape 닫기, 패널 내부 Tab 순환, 닫은 뒤 실행 행 포커스 복귀와 배경 이중 스크롤 차단을 적용했습니다.

## 권한·데이터 안전

- `전체 상세보기`는 기존 `viewApplicant()`, `수정`은 기존 `applicant.write` 검사와 `editApplicant()`를 사용합니다.
- 관리자·채용담당자는 수정할 수 있고 조회 전용은 수정·삭제 동작을 보거나 실행할 수 없습니다.
- 빠른 보기에는 주민등록번호 필드를 참조하거나 렌더링하지 않습니다.
- 열기·닫기·이전·다음은 `save()`, 지원자·사원·학교·일정 배열, 업무 localStorage, 공용저장 Bridge PUT을 호출하지 않습니다.
- 지원자 schema·저장 키·Supabase·RLS·ERP Bridge·공용 master·revision·backup 정책은 변경하지 않았습니다.

## 검사

- 합성 지원자 60명으로 필터·정렬 결과, 30/31번째 페이지 경계, 첫·중간·마지막, 빈 값, 긴 메모와 HTML 문자열 안전 표시를 검사합니다.
- 기존 상세 버튼 분리, Enter 열기, Escape·Shift+Tab 포커스, 전체 상세·수정 연결과 관리자·채용담당자·조회전용 권한을 검사합니다.
- 1920×1080, 1440×900, 1366×768, 1024×768, 768×1024, 390×844와 1366×768 125% 확대에서 패널·버튼 잘림과 본문 overflow를 검사합니다.
- 기존 일반보기·워크시트, 지원자 저장 실패 rollback, 보안·권한·운영 준비·공용저장 회귀검사를 유지합니다.
