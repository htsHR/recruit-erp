# Recruit ERP v10.57.0

## 사용자 권한 관리

- 관리자, 채용담당자, 조회 전용 역할을 추가했습니다.
- `시스템 → 사용자 권한`에서 관리자가 로그인 계정의 역할을 변경할 수 있습니다.
- 현재 로그인 계정의 역할을 상단 사용자 영역에 표시합니다.
- 마지막 관리자 계정은 다른 역할로 변경할 수 없도록 보호합니다.

## 화면과 저장 경로 보호

- 조회 전용 사용자는 지원자 등록·수정·삭제, 일정 변경, 복원 기능을 사용할 수 없습니다.
- 채용담당자는 지원자와 면접·일정을 관리할 수 있지만 삭제, 사원·학교 수정, 백업·복원은 사용할 수 없습니다.
- 지원자, 사원, 학교, 일정, 백업, 문구함의 주요 저장 함수에도 권한 검사를 추가했습니다.
- 관리자 이외의 계정에는 입사대기자 주민등록번호 입력값을 표시하지 않습니다.

## Supabase RLS

- `user_roles` 역할 테이블과 신규 Auth 사용자 자동 등록 트리거를 추가했습니다.
- 지원자 테이블은 모든 역할이 조회할 수 있고 관리자·채용담당자만 등록·수정할 수 있습니다.
- 지원자 삭제, 사원·학교 변경, 클라우드 백업은 관리자만 실행할 수 있습니다.
- 기존의 넓은 허용 정책을 제거한 뒤 v10.57.0 정책만 다시 설치합니다.
- RLS 정책 함수는 노출된 `public`이 아닌 `private` 스키마의 `security definer` 함수로 구성했습니다.

## 설치 필요

배포 후 Supabase SQL Editor에서 `supabase_migration_v10.57.0_rbac_rls.sql`을 한 번 실행해야 서버 권한이 활성화됩니다. 실제 계정이나 개인정보를 자동 테스트에 사용하지 않습니다.
