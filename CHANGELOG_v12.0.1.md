# v12.0.1 로그인 제거·로컬 전용 Hotfix

## 사용자 동작

- 홈페이지 접속 즉시 홈을 열며 이메일·비밀번호·로그인 선택·로그아웃·Supabase 계정 영역을 표시하지 않습니다.
- 상단과 저장 상태에 `LOCAL ONLY` 또는 `이 브라우저 저장`을 낮은 강조도로 표시합니다.
- 기존 브라우저 업무 자료, 공용폴더 Bridge, 지원자·워크시트·검색·필터·라우팅·뒤로가기를 그대로 사용합니다.

## 보안 경계

- 단일 `erpAppVersion.LOCAL_ONLY` 실행 플래그로 `canUseCloud()`를 항상 차단합니다.
- 첫 화면에서 Supabase CDN, `supabase_config.js`, `auth-init.js`를 불러오지 않아 과거 인증 세션의 복원·갱신·자동 동기화가 시작되지 않습니다.
- Supabase 원격 데이터, 인증 계정, DB, RLS, migration과 anon 권한은 변경하지 않았습니다.
- 지원자·사원·학교·일정 localStorage 키, 업무 데이터 구조와 공용 저장 snapshot 구조는 변경하지 않았습니다.

## 초기 화면 안정화

- 문서 파싱 첫 단계부터 밝은 중립 부트 가드를 적용합니다.
- v12 셸·라우트·업무 화면 준비가 끝나기 전에는 기존 셸과 합성 또는 운영 자료를 숨깁니다.
- 모든 변환이 끝난 뒤 앱 셸을 한 번만 표시하고 첫 표시 시점을 성능 표식으로 기록합니다.

## 초기 자산 정리

- 삭제한 파일: 없음.
- `index.html` 참조만 제거: Supabase CDN, `supabase_config.js`, `js/auth-init.js`.
- 유지한 레거시 파일:
  - `supabase_config.js`: 향후 인증 복구 시 기존 접속 계층을 재사용하기 위해 보존합니다.
  - `js/auth-init.js`: 향후 명시적인 인증 재설계 전까지 기존 인증 흐름의 기준 구현으로 보존합니다.
  - `js/cloud-sync.js`: 지원자 삭제 대기·복구 계약과 클라우드 데이터 계층 함수가 함께 있어 기능 회귀 방지를 위해 유지합니다. LOCAL ONLY에서는 네트워크 호출이 차단됩니다.
- CSS 31개와 나머지 기능 JavaScript는 18개 화면의 전역 함수·동적 호출·테스트 의존성이 있어 이번 Hotfix에서 제거하지 않습니다.

## 검사

- 로그인 관련 DOM·접근성 요소 0건과 초기 홈 즉시 진입을 자동 검사합니다.
- 기존 세션을 모의한 상태에서 Supabase 인증·조회·저장·동기화 호출 0회를 확인합니다.
- 1280×720과 1366×768의 100%·125%에서 첫 진입·새로고침·강력 새로고침·직접 라우트를 연속 프레임으로 검사합니다.
- 기존 전체 자동검사, 18개 화면, 3개 역할, 저장 실패 원상복구, Windows Bridge와 5MB 가상 snapshot 검사를 유지합니다.

동일한 Windows·Chrome·캐시 비활성 조건에서 기준본과 변경본을 각각 5회 측정한 중앙값입니다.
초기 참조 수는 `index.html` 정적 경로 기준이며, 실행 후 공용저장 모듈 1개가 동적으로 추가되어 v12.0.1의 브라우저 관측 JavaScript·script 태그는 각각 52개입니다.

| 항목 | v12.0.0 기준본 | v12.0.1 | 변화 |
|---|---:|---:|---:|
| 초기 로컬 CSS 참조 | 31 | 31 | 동일 |
| 초기 로컬 JavaScript 참조 | 52 | 51 | 1개 감소 |
| 초기 script 태그 | 53 | 51 | 2개 감소 |
| 초기 요청 | 87 | 84 | 3개 감소 |
| 응답 본문 합계 | 2,561,337 bytes | 2,341,749 bytes | 219,588 bytes 감소 |
| DOMContentLoaded | 1,042.9ms | 964.9ms | 78.0ms 단축 |
| 첫 v12 셸 표시 | 1,073.5ms | 955.8ms | 117.7ms 단축 |

## 남은 방향

- 클라우드 인증을 다시 제공하려면 로그인 UI만 되살리지 말고 사용자 식별·권한·세션·RLS·운영 UX를 함께 재검토한 별도 버전으로 진행해야 합니다.
- 번들러 도입이나 기능 파일 대량 병합은 이번 Hotfix 범위에서 제외하며 필요 시 v12.1.0 초기 자산 최적화 작업으로 분리합니다.
