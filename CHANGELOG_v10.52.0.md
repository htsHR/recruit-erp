# Recruit ERP v10.52.0

## 개인정보·보안 강화

- 상단의 잠금 버튼으로 지원자·사원 정보가 보이지 않도록 즉시 화면을 가릴 수 있습니다.
- 키보드·마우스 사용이 10분 동안 없으면 개인정보 화면을 자동으로 잠급니다. 화면 잠금은 데이터와 로그인 상태를 변경하지 않습니다.
- 지원자·사원·입사대기자·학교 보고서·JSON 백업 등 개인정보가 포함될 수 있는 파일을 출력하기 전에 저장 위치를 다시 확인합니다.
- 승인한 개인정보 내보내기 종류와 시각을 이 브라우저에 최대 50건까지 기록합니다. 실제 개인정보와 파일 내용은 기록하지 않습니다.
- Vercel 응답에 MIME 스니핑 차단, 외부 프레임 차단, 리퍼러 차단, 불필요한 브라우저 권한 차단 헤더를 추가했습니다.
- 화면 표시 및 정적 자원 버전을 `v10.52.0`으로 갱신했습니다.

## 테스트

- `테스트 지원자 CSV`, 가상 시각 등 실제 개인정보가 없는 값만 사용해 자동 잠금 시간, 내보내기 대상 분류, 보안 기록 보존을 검사합니다.
- 기존 v10.51.0 저장 실패, sparse merge, CSV 수식 방어 회귀 테스트를 함께 실행합니다.

## 알려진 제한

- 화면 잠금은 자리를 잠깐 비울 때 내용을 가리는 보호막입니다. 운영체제 계정 잠금이나 Supabase 재인증을 대신하지 않습니다.
- 현재 화면은 인라인 이벤트와 외부 Supabase 라이브러리를 사용하므로 강한 Content-Security-Policy는 호환성 검증 후 별도 버전에서 적용해야 합니다.
