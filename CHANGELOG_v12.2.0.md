# v12.2.0 채용면접 평가표 순서 편집

- 선택한 면접일의 `면접예정` 지원자 순서를 드래그, 순번 입력, 위·아래 버튼으로 편집할 수 있습니다.
- 저장된 기존 순서는 유지하고, 이후 추가된 면접자는 면접시간과 관계없이 기존 대상 뒤에 배치합니다.
- 사용자가 명시적으로 선택할 때만 유효한 면접시간 오름차순으로 재배치하며 빈 시간은 맨 아래에 둡니다.
- 편집 화면에서 면접시간만 함께 수정하고, 성명 등 다른 정보는 기존 지원자 수정 화면으로 연결합니다.
- 저장 전 대상·날짜·시간·ID를 검증하고 기존 `save()`를 한 번만 호출하며 실패하면 전체 지원자와 브라우저 저장값을 원상복구합니다.
- 조회 전용 사용자는 편집 화면과 직접 저장 호출 모두 차단합니다.
- v12.0.3의 회사명·문구·자동채움·A4 가로·5명 분할·검정 0.35mm 인쇄선을 의미 기반 회귀검사로 보호합니다.
- 모바일·태블릿 신규 UI는 추가하지 않았으며 1280px 이상 데스크톱과 125% 확대를 기준으로 검증합니다.
