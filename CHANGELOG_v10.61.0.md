# v10.61.0 저장소·속도 개선

## 추가

- 지원자, 사원, 협력학교, 일정, 입사대기자, 안내문 템플릿을 IndexedDB에 자동 안전 복사합니다.
- localStorage 저장이 성공한 경우에만 안전 복사 갱신 이벤트를 발생시킵니다.
- 관리자 전용 `저장소·속도` 화면에서 ERP 데이터별 건수와 용량, 브라우저 전체 사용량 추정치, 마지막 안전 복사 시각을 확인할 수 있습니다.
- localStorage가 4MB 이상이거나 브라우저 저장공간 사용률이 80% 이상이거나 남은 추정 공간이 5MB 미만이면 경고합니다.
- 최대 5개의 수동 안전 스냅샷을 IndexedDB의 별도 저장소에 보관하고 오래된 스냅샷은 자동 정리합니다.
- 5,000명의 지원자, 1,000명의 사원, 500개의 학교로 구성한 가상 데이터의 용량 요약 성능 검사를 추가했습니다.
- 모바일 390×844와 1366×768 125% 확대를 포함한 저장소 화면 UI 검사를 추가했습니다.

## 안전 원칙

- 저장소 화면과 콘솔에는 이름, 연락처, 주소, 주민등록번호, 메모 등 행 내용을 출력하지 않습니다.
- IndexedDB로 옮기기 전 기존 JSON 안전 트리 검사와 행 ID 검사를 다시 수행합니다.
- 안전 복사나 스냅샷 생성이 실패해도 기존 ERP 데이터는 변경하지 않습니다.
- 저장소 관리는 관리자와 로컬 관리자에게만 표시합니다. 채용담당자와 조회전용 사용자는 접근할 수 없습니다.
- 실제 개인정보나 운영 데이터는 테스트에 사용하지 않습니다.

## 호환성과 제한

- v10.61.0에서는 기존 동기식 화면과 클라우드 동기화의 회귀를 막기 위해 localStorage를 현재 데이터의 호환 기준으로 유지합니다.
- IndexedDB는 이번 버전에서 안전 복사와 스냅샷 계층입니다. 브라우저를 삭제하거나 같은 기기가 손상되면 함께 사라질 수 있으므로 암호화 파일 백업을 대신하지 않습니다.
- `pagehide` 시 갱신은 브라우저 종료 정책에 따라 완료가 보장되지 않습니다. 일반 저장 성공 직후의 자동 갱신이 기본 보호 수단입니다.
- 데이터 본문 전체를 IndexedDB 단일 원본으로 전환하는 작업은 동기화·복원·초기 로딩의 비동기 전환 검증 후 다음 단계에서 진행합니다.

자세한 운영 기준은 `docs/STORAGE_PERFORMANCE_v10.61.0.md`를 확인하세요.
