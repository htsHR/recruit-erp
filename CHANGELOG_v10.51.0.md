# Recruit ERP v10.51.0

## 안전 업데이트

- 브라우저 저장을 공통 `safeLocalStorageSet` 함수로 처리하고, 저장 실패 시 입력 폼 초기화·목록 이동·클라우드 전송·완료 안내를 중단하도록 핵심 저장 흐름을 보강했습니다.
- 사이드바 저장 상태를 회사 로컬 모드, 클라우드 로그아웃, 동기화 중, 정상 동기화, 동기화 실패로 구분했습니다. 정상 동기화와 실패 상태에는 마지막 성공 시간을 함께 표시합니다.
- 사원 JSON 가져오기에 sparse merge를 적용했습니다. 빈 문자열, `null`, `undefined`, 공백만 있는 값은 기존 정보를 지우지 않습니다.
- 사원 JSON 반영 전에 신규·갱신·변경 없음·건너뜀 건수와 필드별 이전값/새값을 보여주는 변경 미리보기를 추가했습니다.
- 지원자, 사원, 학교 분석·보고서·점검 CSV 내보내기의 `=`, `+`, `-`, `@` 시작값을 일반 텍스트로 처리해 스프레드시트 수식 실행을 방지했습니다.
- 화면 표시 및 정적 자원 버전을 `v10.51.0`으로 갱신했습니다.

## 검증 데이터

- 실제 개인정보나 운영 데이터는 사용하지 않았습니다.
- `테스트사원001`, `홍길동테스트`, `=1+1` 등 가상 값만 사용해 저장 실패, sparse merge, CSV 방어 회귀 테스트를 수행합니다.
