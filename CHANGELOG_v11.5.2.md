# v11.5.2 채용담당자 UI 안정화 마감

## 변경 내용

- 지원자 일반목록 열을 `NO → 성명 → 연락처 → 근무지 → 상태 → 지원일 → 면접일정 → 입사예정일 → 출근방법 → 판정 → 관리` 순으로 정리하고, 넓은 화면의 이메일·지역 열도 같은 colgroup 폭 체계를 사용합니다.
- 고정 열은 NO·성명·연락처로 한정하고 오른쪽 관리 열 고정을 해제해 상태/지원일과 판정/관리 겹침을 제거했습니다.
- 1366·1440·1920px에서 scrollLeft 0/중간/최대를 검사하며 헤더와 본문 경계 오차를 1px 이하로 제한합니다.
- 워크시트 최종 확인의 `중복 후보 N건 · 확인 완료`를 최대 두 개 의미 줄로 유지합니다.
- 오늘 업무의 기존 자동 요약과 KPI를 처리 목록 앞에 표시합니다. 분류기, 우선순위, 중복 제거와 실행 동작은 변경하지 않았습니다.
- 지원자 빠른 보기 상단에 마지막 연락일, 다음 연락 예정일, D-day·기한 경과·미정 상태, 최근 진행 이력 1건을 읽기 전용으로 추가했습니다.
- 기존 상세검색 저장 키와 조건 적용 함수를 재사용하는 `내 보기`를 지원자 목록 툴바에 추가했습니다. 조회 전용도 불러올 수 있지만 저장·삭제 권한은 늘어나지 않습니다.

## 안전 범위

- 신규 업무 필드와 localStorage 키를 추가하지 않았습니다.
- 지원자·사원·학교·일정 배열, 정렬·필터 의미, 진행 이력 및 저장 함수를 변경하지 않았습니다.
- Supabase, DB, migration, RLS, 권한, ERP Bridge API와 공용 저장 구조를 변경하지 않았습니다.
- 자동검사와 화면 artifact에는 실제 개인정보가 없는 합성 자료만 사용합니다.
