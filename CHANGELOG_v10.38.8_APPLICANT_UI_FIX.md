# v10.38.8 APPLICANT_UI_FIX

- 지원자 상세조건 패널을 440px 우측 드로어로 재구성
- 패널 상단 빈 공간과 내부 중복 검색결과 제거
- 기본 조건·추가 조건·기간 조건으로 필터 재분류
- 기간 조건 기본 접힘 처리
- 하단 고정 `취소 / 조건 적용` 버튼 적용
- 지원자 목록 필터와 표 밀도 조정
- 체크박스는 `선택` 버튼을 눌렀을 때만 표시
- 지원자 상세 모달이 화면 밖으로 잘리지 않도록 높이·정렬 수정
- 상세 모달의 중복 상태 진행 UI 제거 및 카드 밀도 완화
- 기존 localStorage 키, JSON 구조, Supabase 저장 로직 변경 없음
