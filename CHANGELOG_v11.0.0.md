# v11.0.0 실제 운영 준비

릴리스 상태: Draft PR 릴리스 후보

## 운영 준비 점검

- 관리자 전용 `운영 준비 점검` 화면을 추가했습니다.
- 화면 버전, 보안 모듈, 암호화 백업, 권한, 변경 이력, 동기화 대기, 저장공간, 가상 6,500건 성능을 자동 확인합니다.
- 권한·RLS, 암호화 복원, 삭제 재생성 방지, 내보내기 감사, 운영자 교육, 장애 복구는 수동 모의훈련으로 분리했습니다.
- 권한·RLS와 유출 비밀번호 보호 확인은 cloud admin 로그인과 클라우드 사용 모드를 모두 충족할 때만 체크할 수 있습니다.
- 수동 확인은 30일 동안만 유효하며 버전이 바뀌면 자동으로 초기화됩니다.
- 동기화·저장공간·암호화·감사·화면 잠금·권한 모듈은 누락 또는 기능 시험 오류 시 fail-closed로 판정합니다.
- 암호화 자동점검은 실제 ERP 데이터가 아닌 가상 문자열로 AES-GCM round-trip을 실행합니다.
- 점검 보고서는 개인정보 원문 없이 검증 출처, 운영 환경, migration·Security Advisor·역할표 확인 여부와 가상 성능 건수만 저장합니다.
- 로컬 보고서는 READY가 될 수 없고, JSON은 전자서명된 증명서가 아닌 수정 가능한 참고 자료입니다.

## Supabase 보안 후보

- 예전 `app_settings`·`allowed_users` 정책 helper를 비공개 `private` schema로 옮기는 migration을 추가했습니다.
- 공개 `SECURITY DEFINER` 함수 4개의 `anon`·`authenticated` 직접 실행 권한을 제거합니다.
- RLS의 `auth.uid()`를 한 번만 평가하도록 정책을 정리하고 `app_settings.updated_by` 인덱스를 추가합니다.
- 이 migration은 PR 승인 전 운영 Supabase에 적용하지 않습니다.
- Supabase Auth 유출 비밀번호 보호는 대시보드에서 운영자가 직접 켜야 합니다.
- 적용 DDL, 읽기 전용 검증 SQL, 관리자 승인용 rollback SQL을 서로 다른 파일로 분리했습니다.

## 운영 문서

- 일일 업무와 정기 점검을 설명하는 운영자 설명서
- 저장·동기화·삭제·복원·개인정보·배포 장애 복구 절차
- 자동검사와 사람의 모의훈련을 나눈 릴리스 준비 점검표

## 데이터 영향

- 지원자·사원·학교·일정·입사대기 데이터 구조는 변경하지 않습니다.
- 새 Supabase 표를 만들지 않습니다.
- 새 브라우저 저장 키 `recruit_erp_production_readiness_v1100`에는 점검 ID, 시각, 역할과 가상 성능 건수만 저장합니다.
- 실제 개인정보나 운영 백업을 테스트에 사용하지 않습니다.
