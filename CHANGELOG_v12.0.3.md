# v12.0.3 - 채용 면접 평가표 인쇄선 대비 보정

## 변경 사항

- 채용 면접 평가표 전용 인쇄선 색상을 순흑색 `#000`, 물리 굵기를 `0.35mm`로 통일했습니다.
- 표 외곽선, 모든 셀, 성명 머리글 구분선, 서약 상자에 같은 전용 CSS 변수를 적용했습니다.
- 인쇄 대상에 정확한 색상 출력을 지정하고 기존 `border-collapse: collapse`를 유지했습니다.
- 변경된 `app-version.js`와 `components.css`만 `v12.0.3` 캐시 버전으로 갱신했습니다.

## 변경하지 않은 범위

- 브라우저 인쇄 머리글·바닥글을 숨기기 위한 코드나 `@page` 여백 변경은 포함하지 않았습니다.
- 면접표 문구, 자동입력 항목, 열 너비, 행 높이, 한 페이지 5명 분할, A4 가로 설정과 `js/reports.js`는 변경하지 않았습니다.
- 실제 업무 데이터, 저장소, 권한, Bridge 및 원격 데이터베이스 구성은 변경하지 않았습니다.

## 검증

- 개인정보 없는 가상 지원자 0·1·5·6·10명으로 페이지 수와 5명 분할을 검사합니다.
- Chromium PDF는 A4 가로, 배율 100%, 브라우저 머리글·바닥글 끔 조건으로 확인합니다.
- 전체 문법·자동 테스트와 Playwright UI 레이아웃 검사를 유지합니다.
