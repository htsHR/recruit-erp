# v11.3.0 지원자 워크시트 1단계

## 워크시트 보기

- 기존 지원자 일반보기와 분리된 `일반보기 / 워크시트 보기` 전환을 추가했습니다.
- 현재 필터·정렬·페이지의 행만 NO, 성명, 연락처와 9개 기존 지원자 필드로 표시합니다.
- NO와 성명을 고정하고 현재 셀·행, 임시 변경 셀, 검증 오류 셀을 구분합니다.
- Enter·타이핑 편집, Tab·Shift+Tab, Enter 다음 행, 방향키 이동과 직사각형 복사/붙여넣기를 지원합니다.
- 현재 페이지와 편집 가능 열을 벗어나는 붙여넣기는 부분 적용 없이 전부 취소합니다.

## 저장 안전

- 저장 전 변경은 지원자 배열, 업무 localStorage, 클라우드와 공용 master를 건드리지 않고 메모리 변경목록에만 유지합니다.
- 날짜·시간·상태 필수일·일정 순서·선택값을 전체 검증하고 오류가 하나라도 있으면 저장을 비활성화합니다.
- 저장 직전 전체 지원자 배열을 복제하고 변경된 9개 필드와 `updatedAt`만 합친 뒤 기존 `normalize()`와 `save()`를 정확히 한 번 사용합니다.
- 저장 실패 시 지원자 배열과 브라우저 저장값을 저장 전 상태로 복원하고 성공 안내나 별도 감사로그를 만들지 않습니다.
- 보기·필터·정렬·페이지 이동 전에는 저장, 변경 취소, 계속 편집 중 하나를 선택합니다.

## 권한·개인정보

- `applicant.read` 범위에서 조회하고 `applicant.write`가 없으면 편집·붙여넣기·저장을 차단합니다.
- 별도 UI 설정에는 `viewMode`, `currentWorkplace`, `currentFilter`, `currentSort`, `hideFinished`, `applicantPageSize`만 저장합니다.
- 검색어, 학교·고급필터 ID, 지원자 ID나 개인정보는 UI 설정에 저장하지 않습니다.
- 합성 지원자 데이터만 테스트했으며 Supabase·DB·권한·Bridge·공용저장 구조는 변경하지 않았습니다.

## 검증

- 워크시트 열·설정 allowlist·직사각형 붙여넣기·검증·정규화·원상복구를 자동 검사합니다.
- 1366×768, 1440×900, 1920×1080과 1366×768 125% 확대에서 내부 스크롤, 고정 열, 저장 1회, 실패 복구와 조회 전용 차단을 검사합니다.
