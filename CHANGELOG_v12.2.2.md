# v12.2.2 평가표 순서 편집·인쇄 연결

- 채용면접 평가표 순서 편집창에 `평가표 보기/인쇄` 버튼을 추가했습니다.
- 순서 또는 면접시간 변경이 있으면 기존 저장 절차를 정확히 한 번 실행한 뒤에만 평가표를 엽니다.
- 저장 실패 시 인쇄를 시작하지 않고 편집창·초안·기존 브라우저 데이터를 그대로 유지합니다.
- 변경이 없으면 불필요한 저장 없이 현재 저장 순서의 평가표를 바로 엽니다.
- 데스크톱 신규 UI만 적용하고 모바일·태블릿 신규 CSS와 QA는 추가하지 않았습니다.
