# v11.2.0 입사대기 업무 자동화

## 사번·비고 자동작성

- 선택한 날짜가 포함된 입사월 전체의 입사예정자를 대상으로 `SYYMM001` 형식 사번을 미리 계산합니다.
- 기존 입사대기 프로필과 사원명부에서 이미 사용 중인 사번을 대소문자 구분 없이 제외하고, 가장 작은 빈 순번을 사용합니다.
- 신규 대상은 완전한 생년월일, 이름, 지원자 ID 순으로 고정 정렬하며 주민등록번호는 채번에 사용하지 않습니다.
- 기존 수기 사번과 수기 비고는 변경하지 않습니다. 경력직은 경력사항을, 신입은 유효한 키·체중 두 값이 모두 있을 때만 비고를 자동작성합니다.
- 키와 체중은 미리보기에서만 입력하고 적용 전에는 저장하지 않습니다. 적용할 때 선택 필드 `heightCm`, `weightKg`로 입사대기 프로필에만 저장합니다.

## 안전성

- 미리보기와 취소는 지원자·사원·입사대기 배열, localStorage, 감사로그 및 공용 저장소를 변경하지 않습니다.
- 적용 시 `employee.write` 권한과 사번·날짜·입력값을 다시 검사한 뒤 입사대기 프로필을 한 번만 저장합니다.
- 저장에 실패하면 메모리 배열을 바꾸지 않아 부분 반영 없이 변경 전 상태를 유지합니다.
- 기존 localStorage와 공용폴더 저장 이벤트를 그대로 사용하며 Bridge, Supabase, RLS, 공용 저장 schemaVersion은 변경하지 않습니다.

## 자동 검사

- 월 전체 채번, 빈 순번 재사용, 사원명부 중복 회피, 기존 사번·비고 보존, 잘못된 생년월일 제외를 검사합니다.
- 경력·신입 비고, 한쪽만 입력한 키·체중, 미리보기 무변경, 저장 실패 rollback, 반복 계산 결정성을 검사합니다.
- 기존 수기 표·붙여넣기·저장·XLSX·온보딩과 공용폴더 저장 회귀 검사를 유지합니다.
